**Exercise 05 (06 Oct 20)**
- creating colour and storing it into a variable using the color() function
- using the lerpColor() function to interpolate two colours and find a third colour between them
